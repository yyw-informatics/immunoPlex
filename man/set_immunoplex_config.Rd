% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.r
\name{set_immunoplex_config}
\alias{set_immunoplex_config}
\title{Set immunoPlex configuration}
\usage{
set_immunoplex_config(
  random_effects_min_subjects = NULL,
  random_effects_min_reps = NULL,
  high_censoring_threshold = NULL,
  high_skewness_threshold = NULL,
  default_dharma_nsim = NULL,
  plotting_point_size = NULL,
  plotting_alpha_cens = NULL,
  ancova_iqr_multiplier = NULL,
  ancova_min_n = NULL,
  ancova_fdr_method = NULL,
  ancova_fdr_threshold = NULL,
  ancova_prevalence_threshold = NULL,
  glmmTMB_gradient_warn = NULL,
  glmmTMB_gradient_fail = NULL,
  glmmTMB_loo_influence_threshold = NULL,
  replicate_cv_threshold = NULL,
  replicate_mad_threshold = NULL,
  replicate_flag_rule = NULL,
  replicate_action = NULL,
  replicate_well_detection = NULL,
  replicate_well_params = NULL
)
}
\arguments{
\item{random_effects_min_subjects}{Minimum number of subjects required for random effects (default: 30)}

\item{random_effects_min_reps}{Minimum replicates per subject for random effects (default: 3)}

\item{high_censoring_threshold}{Percentage above which to prefer Tobit models (default: 70)}

\item{high_skewness_threshold}{Skewness above which to prefer Gamma models (default: 2)}

\item{default_dharma_nsim}{Default number of DHARMa simulations (default: 1000)}

\item{plotting_point_size}{Default point size for plots (default: 1.2)}

\item{plotting_alpha_cens}{Default alpha for censored points (default: 0.7)}

\item{ancova_iqr_multiplier}{IQR multiplier for ANCOVA outlier removal (default: 3)}

\item{ancova_min_n}{Minimum observations per analyte for ANCOVA (default: 12)}

\item{ancova_fdr_method}{FDR correction method for ANCOVA (default: "BH")}

\item{ancova_fdr_threshold}{FDR significance threshold for ANCOVA (default: 0.05)}

\item{ancova_prevalence_threshold}{Minimum prevalence for conditional covariates (default: 0.03)}

\item{glmmTMB_gradient_warn}{Gradient threshold for convergence warning (default: 0.001)}

\item{glmmTMB_gradient_fail}{Gradient threshold for convergence failure (default: 0.01)}

\item{glmmTMB_loo_influence_threshold}{Percent change threshold for LOO influential subject detection (default: 20)}

\item{replicate_cv_threshold}{CV threshold (\%) for replicate QC flagging (default: 25)}

\item{replicate_mad_threshold}{MAD multiplier for replicate QC flagging (default: 3)}

\item{replicate_flag_rule}{Flag rule for replicate QC: "cv", "mad", "either", "both" (default: "cv")}

\item{replicate_action}{Action for flagged replicates: "flag", "drop_both", "drop_farther", "winsorize" (default: "flag")}

\item{replicate_well_detection}{Well detection mode: "analyte" (per-analyte) or "sample" (sample-level well failure detection) (default: "analyte")}

\item{replicate_well_params}{Named list of well detection tuning parameters (default: list())}
}
\description{
Convenience function to set multiple immunoPlex configuration parameters at once.
Individual parameters can also be set using base R \code{options()}.
}
\examples{
# Adjust thresholds for small sample sizes
set_immunoplex_config(
  random_effects_min_subjects = 20,
  random_effects_min_reps = 2
)

# Modify censoring threshold for model selection
set_immunoplex_config(high_censoring_threshold = 50)

# Faster DHARMa for interactive use
set_immunoplex_config(default_dharma_nsim = 250)

}
