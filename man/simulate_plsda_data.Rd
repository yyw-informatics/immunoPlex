% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_scenarios.r
\name{simulate_plsda_data}
\alias{simulate_plsda_data}
\title{Simulate PLS-DA classification data}
\usage{
simulate_plsda_data(
  n_subjects = 60,
  n_analytes = 20,
  n_discriminatory = 5,
  group_effects = 0.7,
  analyte_correlation = "block",
  block_rho = 0.5,
  class_imbalance = NULL,
  lod_quantile = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_subjects}{Integer. Total subjects. Default 60.}

\item{n_analytes}{Integer. Total analytes (discriminatory + noise). Default 20.}

\item{n_discriminatory}{Integer. Number of truly discriminatory analytes.
Default 5.}

\item{group_effects}{Numeric. Log-scale effect size for discriminatory
analytes. Default 0.7.}

\item{analyte_correlation}{Character or NULL. \code{"block"} for block
correlation, NULL for independent. Default \code{"block"}.}

\item{block_rho}{Numeric. Within-block correlation. Default 0.5.}

\item{class_imbalance}{Numeric or NULL. If numeric, specifies the ratio of
group 1 to group 2 (e.g., 2 for 2:1). NULL for balanced. Default NULL.}

\item{lod_quantile}{Numeric or NULL. LOD quantile for censoring. Default NULL
(use library LOD values).}

\item{seed}{Integer or NULL. RNG seed.}

\item{...}{Additional arguments passed to \code{\link{simulate_immunoassay}}.}
}
\value{
An \code{\link{new_immuno_sim}} object.
}
\description{
Generates data with explicit discriminatory vs. noise analytes for PLS-DA
validation. Supports block correlation, class imbalance, and control over
signal strength.
}
\examples{
\dontrun{
sim <- simulate_plsda_data(n_subjects = 50, n_discriminatory = 5, seed = 42)
print(sim)
}

}
