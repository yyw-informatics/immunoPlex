% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_loo.r
\name{leave_one_out_sensitivity}
\alias{leave_one_out_sensitivity}
\title{Leave-one-out sensitivity analysis}
\usage{
leave_one_out_sensitivity(
  data,
  fixed,
  target_terms,
  random = "(1|subject_id)",
  family = "gaussian",
  dispformula = ~1,
  subject_col = NULL,
  influence_threshold = NULL,
  progress = TRUE
)
}
\arguments{
\item{data}{A data frame containing all observations.}

\item{fixed}{Character string of fixed effects formula.}

\item{target_terms}{Character vector of coefficient names to assess
(e.g. \code{c("timepointAcute", "timepointConvalescent")}).}

\item{random}{Character string of random effects. Default
\code{"(1|subject_id)"}.}

\item{family}{Character family for \code{fit_one()}. Default
\code{"gaussian"}.}

\item{dispformula}{Dispersion formula passed to \code{fit_one()}.
Default \code{~1}.}

\item{subject_col}{Character name of the column defining the LOO
grouping (i.e. which rows to exclude together). Default
\code{"subject_id"}.}

\item{influence_threshold}{Numeric percent change threshold to flag
a subject as influential. Default 20 (i.e. 20\%).}

\item{progress}{Logical; if \code{TRUE} (default), prints progress
messages.}
}
\value{
A tibble with columns: \code{excluded_subject}, \code{term},
\code{full_estimate}, \code{loo_estimate}, \code{difference},
\code{pct_change}, \code{influential}.
}
\description{
For each unique subject in the data, refits the model with that subject
excluded and records the change in coefficient estimates for the target
terms. Subjects whose removal changes an estimate by more than
\code{influence_threshold} percent are flagged as influential.
}
\details{
Uses \code{\link{fit_one}} internally, inheriting the optimizer cascade
and convergence checking.
}
