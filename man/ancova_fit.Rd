% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ancova_fit.r
\name{ancova_fit}
\alias{ancova_fit}
\title{Fit Rank-Based ANCOVA Across Multiple Analytes}
\usage{
ancova_fit(
  data,
  outcome,
  group,
  covariate,
  analyte_col = "cytokine",
  covariates = NULL,
  conditional_covariates = NULL,
  prevalence_threshold = getOption("immunoplex.ancova_prevalence_threshold", 0.03),
  outlier_removal = TRUE,
  iqr_multiplier = getOption("immunoplex.ancova_iqr_multiplier", 3),
  min_n = getOption("immunoplex.ancova_min_n", 12L),
  conf_level = 0.95,
  fdr_method = getOption("immunoplex.ancova_fdr_method", "BH"),
  fdr_threshold = getOption("immunoplex.ancova_fdr_threshold", 0.05),
  log_validate = FALSE,
  rep_col = NULL,
  plate_col = NULL,
  quiet = FALSE
)
}
\arguments{
\item{data}{A data frame in long format with one row per subject-analyte
combination, containing outcome, group, covariate, and analyte columns.}

\item{outcome}{Character. Column name for the outcome variable.}

\item{group}{Character. Column name for the group factor.}

\item{covariate}{Character. Column name for the primary covariate.}

\item{analyte_col}{Character. Column name identifying the analyte/cytokine.
Default \code{"cytokine"}.}

\item{covariates}{Character vector. Additional fixed covariates. Default \code{NULL}.}

\item{conditional_covariates}{Character vector. Binary covariates included
conditionally based on prevalence. Default \code{NULL}.}

\item{prevalence_threshold}{Numeric. Minimum prevalence for conditional
covariates. Default from config (0.03).}

\item{outlier_removal}{Logical. Apply IQR outlier removal. Default \code{TRUE}.}

\item{iqr_multiplier}{Numeric. IQR multiplier for outlier fencing. Default
from config (3).}

\item{min_n}{Integer. Minimum observations per analyte. Default from config (12).}

\item{conf_level}{Numeric. Confidence level. Default 0.95.}

\item{fdr_method}{Character. FDR correction method passed to \code{p.adjust()}.
Default from config (\code{"BH"}).}

\item{fdr_threshold}{Numeric. Significance threshold for FDR-corrected
p-values. Default from config (0.05).}

\item{log_validate}{Logical. Also fit log-scale validation models.
Default \code{FALSE}.}

\item{rep_col}{Optional character. Column name identifying technical
replicates within a subject. Forwarded to \code{\link{ancova_one}};
when supplied, each per-analyte fit switches to \code{glmmTMB::glmmTMB()}
with a nested random intercept \code{(1 | subject_id:rep_col)}.
Default \code{NULL}. See \code{\link{aggregate_replicates}} for a
pre-aggregation alternative.}

\item{plate_col}{Optional character. Column name identifying plates.
Forwarded to \code{\link{ancova_one}}; when supplied, each per-analyte
fit appends a flat random intercept \code{(1 | plate_col)}.
Default \code{NULL}.}

\item{quiet}{Logical. Suppress progress messages. Default \code{FALSE}.}
}
\value{
An S3 object of class \code{"immuno_ancova_set"}, a list containing:
\describe{
\item{results}{A \code{tibble} with one row per analyte and columns for
all statistics, plus \code{q_value} (FDR-corrected p-value) columns}
\item{models}{Named list of \code{immuno_ancova} objects, one per analyte}
\item{n_analytes}{Integer: total analytes analyzed}
\item{n_significant}{Integer: count with q-value below \code{fdr_threshold}}
\item{fdr_method}{Character: FDR method used}
\item{fdr_threshold}{Numeric: significance threshold}
\item{call}{The matched call}
\item{config}{List: config snapshot for reproducibility}
}
}
\description{
Iterates \code{\link{ancova_one}} over analytes in a long-format data frame,
applies FDR correction, and returns a structured result set.
}
\examples{
\dontrun{
results <- ancova_fit(
  data = my_long_data,
  outcome = "cord_value",
  group = "covid_status",
  covariate = "maternal_value",
  analyte_col = "cytokine",
  covariates = c("age", "gest_delivery"),
  conditional_covariates = c("HIV", "malaria"),
  log_validate = TRUE
)
print(results)
summary(results)
}

}
