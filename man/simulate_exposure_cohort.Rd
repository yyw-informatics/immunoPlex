% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_scenarios.r
\name{simulate_exposure_cohort}
\alias{simulate_exposure_cohort}
\title{Simulate an exposure cohort study}
\usage{
simulate_exposure_cohort(
  n_subjects = 90,
  n_analytes = 20,
  signal_analytes = 5,
  group_effects = 0.7,
  covariate_group_cor = 0.3,
  lod_scale = 2.5,
  visit_missingness = 0.05,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_subjects}{Integer. Total number of subjects. Default 90.}

\item{n_analytes}{Integer. Number of analytes. Default 20.}

\item{signal_analytes}{Integer, logical, or character vector. Default 5.}

\item{group_effects}{Numeric. Log-scale exposure effect. Default 0.7.}

\item{covariate_group_cor}{Numeric. Correlation between covariates and group,
creating genuine confounding. Default 0.3.}

\item{lod_scale}{Numeric. Multiplier applied to built-in analyte library LODs
to increase overall censoring. Default 2.5 (produces heavy censoring in
unexposed group while preserving per-analyte heterogeneity).}

\item{visit_missingness}{Numeric. Per-visit random missingness probability
from scheduling/logistics. Default 0.05 (~5\% missing visits).}

\item{seed}{Integer or NULL. RNG seed.}

\item{...}{Additional arguments passed to \code{\link{simulate_immunoassay}}.}
}
\value{
An \code{\link{new_immuno_sim}} object.
}
\description{
Paired-exposure design with 3 groups (unexposed, low, high) and household
clustering. Uses built-in analyte library LODs for realistic heterogeneous
per-analyte censoring, with LODs scaled up to produce heavier censoring
in unexposed groups. Features confounding covariates (age, sex).
}
\examples{
\dontrun{
sim <- simulate_exposure_cohort(n_subjects = 60, seed = 42)
print(sim)
}

}
