% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-immuno_fit.r
\name{plot.immuno_fit}
\alias{plot.immuno_fit}
\title{Plot residual diagnostics for immuno_fit objects}
\usage{
\method{plot}{immuno_fit}(
  x,
  plot_type = c("basic", "censor_aware"),
  dharma = FALSE,
  nsim = getOption("immunoplex.dharma_nsim", 1000),
  point_size = getOption("immunoplex.point_size", 1.2),
  alpha_cens = getOption("immunoplex.alpha_cens", 0.6),
  save_pdf = TRUE,
  ...
)
}
\arguments{
\item{x}{An \code{immuno_fit} object from \code{fit_one()}.}

\item{plot_type}{Character. Either "basic" (default) for standard plots or
"censor_aware" for censoring-aware visualization.}

\item{dharma}{Logical. If TRUE, includes DHARMa residual diagnostics.}

\item{nsim}{Integer. Number of simulations for DHARMa (default from config, typically 1000).}

\item{point_size}{Numeric. Size of points in plots (default 1.2).}

\item{alpha_cens}{Numeric. Transparency for censored points (default 0.6).}

\item{save_pdf}{Logical. Whether to save plots as PDF (default TRUE).}

\item{...}{Additional arguments (currently unused).}
}
\value{
Invisibly returns a list containing:
\itemize{
\item \code{combined_plot}: The patchwork combined plot
\item \code{rvf_plot}: Residuals vs fitted plot
\item \code{qq_plot}: Q-Q plot
\item \code{dharma_obj}: DHARMa object (if dharma=TRUE)
}
}
\description{
Creates residual diagnostic plots for fitted models with optional censoring
awareness. Supports randomized quantile residuals for censored models and
DHARMa simulated diagnostics.
}
\details{
For censored models (tobit/aft), randomized quantile residuals are used when
\code{statmod} is available, otherwise deviance residuals are used. Censoring-aware
plots color points by censoring status and add interpretive elements.
}
\examples{
\dontrun{
fit <- fit_one(data, family = "tobit")
plot(fit)  # Basic plots
plot(fit, plot_type = "censor_aware")  # Censoring-aware
plot(fit, dharma = TRUE)  # Include DHARMa diagnostics
}

}
