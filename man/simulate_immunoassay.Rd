% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_immunoassay.r
\name{simulate_immunoassay}
\alias{simulate_immunoassay}
\title{Simulate immunoassay data}
\usage{
simulate_immunoassay(
  n_subjects = 60,
  n_timepoints = 2,
  n_analytes = 20,
  design = "pre_post",
  group_levels = c("control", "treatment"),
  group_allocation = NULL,
  group_effects = 0.5,
  time_effects = NULL,
  interaction_effects = NULL,
  signal_analytes = 5,
  effect_direction = "mixed",
  re_intercept_sd = 0.5,
  re_slope_sd = 0,
  residual_sd = 1,
  residual_ar1 = 0,
  heteroscedastic = FALSE,
  analyte_means = NULL,
  analyte_sds = NULL,
  analyte_correlation = NULL,
  block_rho = 0.5,
  covariates = NULL,
  covariate_group_cor = 0,
  dropout_rate = 0,
  dropout_informative = FALSE,
  visit_missingness = 0,
  lod_values = NULL,
  ulod_values = NULL,
  lod_quantile = NULL,
  n_plates = 1,
  plate_sd = 0,
  n_lots = 1,
  lot_sd = 0,
  bg_shape = 0,
  bg_rate = 1,
  n_replicates = 1,
  replicate_sd = 0,
  well_failure_rate = 0,
  cv_threshold = 0.25,
  covariate_missingness = 0,
  format = "long",
  seed = NULL
)
}
\arguments{
\item{n_subjects}{Integer. Total number of subjects. Default 60.}

\item{n_timepoints}{Integer. Number of time points. Default 2.}

\item{n_analytes}{Integer. Number of analytes. Default 20.}

\item{design}{Character. One of \code{"pre_post"}, \code{"time_course"},
\code{"cross_sectional"}, \code{"paired_exposure"}. Default
\code{"pre_post"}.}

\item{group_levels}{Character vector. Group names. Default
\code{c("control", "treatment")}.}

\item{group_allocation}{Numeric vector or NULL. Allocation ratio.
Default NULL (balanced).}

\item{group_effects}{Numeric. Log-scale group effect per signal analyte.
Default 0.5.}

\item{time_effects}{Numeric vector or NULL. Log-scale time effects.}

\item{interaction_effects}{Numeric vector or NULL. Log-scale interaction
effects.}

\item{signal_analytes}{Integer, logical vector, or character vector. Which
analytes carry true signal. Integer = count (random selection). Default 5.}

\item{effect_direction}{Character. \code{"up"}, \code{"down"}, or
\code{"mixed"}. Default \code{"mixed"}.}

\item{re_intercept_sd}{Numeric. Between-subject SD (log). Default 0.5.}

\item{re_slope_sd}{Numeric. Random slope SD. Default 0 (no random slopes).}

\item{residual_sd}{Numeric or named numeric. Within-subject SD (log).
Default 1.0.}

\item{residual_ar1}{Numeric. AR(1) correlation for within-subject residuals.
Default 0.}

\item{heteroscedastic}{Logical. Allow variance to differ by group? Default
FALSE.}

\item{analyte_means}{Named numeric or NULL. Per-analyte baseline log-means.}

\item{analyte_sds}{Named numeric or NULL. Per-analyte log-SDs.}

\item{analyte_correlation}{Matrix, character (\code{"block"}/\code{"toeplitz"}),
or NULL. Default NULL (independent).}

\item{block_rho}{Numeric. Within-block correlation when
\code{analyte_correlation = "block"}. Default 0.5.}

\item{covariates}{List or NULL. Named list of covariate specifications,
each a list with elements \code{type} (\code{"continuous"} or
\code{"binary"}) and \code{effect} (numeric).}

\item{covariate_group_cor}{Numeric. Correlation between covariates and
group assignment (for confounding). Default 0.}

\item{dropout_rate}{Numeric. Per-timepoint dropout probability (monotone).
Default 0.}

\item{dropout_informative}{Logical. Informative dropout? Default FALSE.}

\item{visit_missingness}{Numeric. Per-visit random missingness probability
(non-monotone). Default 0.}

\item{lod_values}{Named numeric or NULL. Per-analyte LOD in pg/mL.}

\item{ulod_values}{Named numeric or NULL. Per-analyte ULOD in pg/mL.}

\item{lod_quantile}{Numeric or NULL. Set LOD at this quantile of the
marginal distribution.}

\item{n_plates}{Integer. Number of plates/batches. Default 1.}

\item{plate_sd}{Numeric. Plate effect SD (log-scale). Default 0.}

\item{n_lots}{Integer. Number of reagent lots. Default 1.}

\item{lot_sd}{Numeric. Lot effect SD (log-scale). Default 0.}

\item{bg_shape}{Numeric. Background binding Gamma shape (0 = none).
Default 0.}

\item{bg_rate}{Numeric. Background binding Gamma rate. Default 1.}

\item{n_replicates}{Integer. Technical replicates per observation. Default 1.}

\item{replicate_sd}{Numeric. Replicate-to-replicate SD (log-scale).
Default 0.}

\item{well_failure_rate}{Numeric. Fraction of wells that fail. Default 0.}

\item{cv_threshold}{Numeric. CV threshold for high-CV QC flag. Default 0.25.}

\item{covariate_missingness}{Numeric. Fraction of covariate values set to
NA. Default 0.}

\item{format}{Character. Output format: \code{"long"} (default).}

\item{seed}{Integer or NULL. RNG seed for reproducibility.}
}
\value{
An \code{\link{new_immuno_sim}} S3 object with components
\code{$data} (observed tibble), \code{$truth} (ground-truth tibble),
and \code{$meta} (simulation parameters and realized summaries).
}
\description{
Single entry point for generating simulated multiplex immunoassay data.
Calls \code{simulate_latent_biology()} to generate latent biological
concentrations, \code{apply_assay_process()} to apply measurement effects
(plates, lots, censoring, replicates, QC failures), and
\code{format_for_immunoplex()} to format the output for downstream
immunoPlex analysis functions.
}
\details{
\strong{Artifact-induced bias cannot be fixed downstream.} Benchmark B0
shows that when plate, lot, and background-binding artifacts are all
present simultaneously (\code{plate_sd > 0}, \code{lot_sd > 0},
\code{bg_shape > 0}), every downstream statistical family -- Tobit,
Gaussian glmmTMB, LOD/2 substitution, and Gamma -- exhibits negative
bias of roughly -0.09 to -0.17 in the estimated group effect, even
when Tobit is superior on clean data. Adding plate fixed effects inside
the model does not recover the full effect because plate indicators alone
do not capture lot effects or background binding. Experimental design and
preprocessing (lot harmonization, background subtraction, plate
balance) must address these artifacts upstream; no statistical method in
this package can fully correct for them.
}
\examples{
\dontrun{
# Simple cross-sectional simulation
sim <- simulate_immunoassay(
  n_subjects = 40, n_timepoints = 1, n_analytes = 10,
  design = "cross_sectional", signal_analytes = 3, seed = 42
)
print(sim)

# Pre-post design with plate effects and censoring
sim <- simulate_immunoassay(
  n_subjects = 60, design = "pre_post",
  n_plates = 3, plate_sd = 0.1, seed = 123
)
summary(sim)
}

}
