% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_cytokine_data.r
\name{prepare_cytokine_data}
\alias{prepare_cytokine_data}
\title{Prepare cytokine data for modeling}
\usage{
prepare_cytokine_data(cytokine_name, data_source = NULL)
}
\arguments{
\item{cytokine_name}{Character. Name of the cytokine to extract.
Use names(immunoplex_example$expression) to see available options.}

\item{data_source}{List. Source data with components 'expression',
'metadata', and 'lod_lookup'. Defaults to immunoplex_example.}
}
\value{
A data.frame suitable for immunoPlex modeling functions with columns:
\itemize{
\item sample_id: Sample identifiers
\item subject_id: Subject identifiers
\item value: Cytokine concentration values
\item timepoint: Timepoint factor
\item disease: Disease status factor
\item age: Age covariate
\item lod: Limit of detection value
\item ulod: Upper limit of detection (if available)
\item cens_lod: Logical flag for left-censored observations
\item cens_ulod: Logical flag for right-censored observations
\item any other columns present in \code{data_source$metadata} (e.g.
\code{replicate}, \code{plate}, \code{preexist}) are passed through, so they
can be supplied to \code{fit_one()} / \code{ancova_one()} via \code{rep_col} /
\code{plate_col}.
}
}
\description{
Helper function to extract and prepare data for a single cytokine
from the immunoplex_example dataset for use with fit_one(), fit_models(),
or compare_lod_models().
}
\examples{
\dontrun{
# Load example data
data("immunoplex_example", package = "immunoPlex")

# Prepare data for one cytokine
dat <- prepare_cytokine_data("Cytokine_01")

# Fit a single model
fit <- fit_one(dat, family = "gamma")

# Compare multiple models
models <- fit_models(dat, families = c("gamma", "tobit", "tobit_censreg"))

# Compare LOD handling approaches
comparison <- compare_lod_models(dat, lod_methods = c("half", "halfmin"))
}

}
