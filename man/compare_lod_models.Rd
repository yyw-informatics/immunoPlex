% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare_lod_models.r
\name{compare_lod_models}
\alias{compare_lod_models}
\title{Compare LOD-handling model families}
\usage{
compare_lod_models(
  dat,
  families = c("gamma", "tobit", "aft"),
  lod_methods = c("half", "halfmin"),
  include_log_transform = TRUE,
  fixed = "timepoint*disease + age",
  random = "(1|subject_id)",
  ...
)
}
\arguments{
\item{dat}{Data frame from prepare_cytokine_data() or similar, with
columns \code{value}, \code{cens_lod} and \code{lod}. Unlike \code{\link{fit_one}},
this function expects \code{value} on the \strong{raw concentration scale},
because the LOD-substitution strategies it tests are defined in raw
units (e.g. LOD/2). It log-transforms internally where the fitted
family requires it, so pass raw values and do not pre-log.}

\item{families}{Character vector of model families to compare.
Options: "gamma", "tobit", "tobit_censreg", "aft"}

\item{lod_methods}{Character vector of LOD preprocessing strategies to test
with gamma models. Options: "half", "zero", "sqrt", "lod", "halfmin"}

\item{include_log_transform}{Logical. Whether to include log-transformed
versions of preprocessing methods.}

\item{fixed}{Fixed effects formula string}

\item{random}{Random effects formula string}

\item{...}{Additional arguments passed to fit_one()}
}
\value{
An object of class \code{immuno_lod_comparison} containing:
\itemize{
\item Preprocessed model results (gamma with different LOD methods)
\item Censoring-aware model results (tobit, AFT)
\item Overall comparison table
\item Best model recommendation
}
}
\description{
High-level helper that compares different approaches to handling
limit-of-detection (LOD) values: preprocessing strategies vs.
censoring-aware models.
}
\examples{
\dontrun{
# Load and prepare data
data("immunoplex_example", package = "immunoPlex")
dat <- prepare_cytokine_data("Cytokine_01")

# Compare LOD handling approaches
comparison <- compare_lod_models(
  dat, 
  families = c("gamma", "tobit", "tobit_censreg"),
  lod_methods = c("half", "halfmin"),
  include_log_transform = TRUE
)

print(comparison)
plot(comparison)
}
}
