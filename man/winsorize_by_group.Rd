% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_modeling.r
\name{winsorize_by_group}
\alias{winsorize_by_group}
\title{Winsorize outliers within groups using IQR bounds}
\usage{
winsorize_by_group(
  data,
  value_col = "value",
  group_cols = c("cytokine", "timepoint"),
  iqr_mult = 3,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing the value and grouping columns.}

\item{value_col}{Character name of the column to winsorize. Default
\code{"value"}.}

\item{group_cols}{Character vector of column names defining groups.
Default \code{c("cytokine", "timepoint")}.}

\item{iqr_mult}{Numeric multiplier for IQR to set bounds. Default 3.}

\item{verbose}{Logical; if \code{TRUE} (default), prints outlier count.}
}
\value{
The data frame with outlier values in \code{value_col} replaced
by group-specific IQR bounds.
}
\description{
For each combination of grouping columns, computes the IQR and caps
values beyond \code{iqr_mult * IQR} from Q1/Q3. This is a robust
approach to handling extreme values in repeated-measures cytokine data
where outliers may be group-specific.
}
\examples{
\dontrun{
cleaned <- winsorize_by_group(
  data = my_data,
  value_col = "value",
  group_cols = c("cytokine", "timepoint"),
  iqr_mult = 3
)
}

}
