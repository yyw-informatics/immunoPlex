% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_scenarios.r
\name{simulate_ancova_data}
\alias{simulate_ancova_data}
\title{Simulate ANCOVA data with confounding}
\usage{
simulate_ancova_data(
  n_subjects = 60,
  n_analytes = 10,
  signal_analytes = 3,
  group_effects = 0.5,
  covariate_group_cor = 0.4,
  heteroscedastic = FALSE,
  outlier_rate = 0,
  nonlinear_cov = FALSE,
  seed = NULL,
  ...
)
}
\arguments{
\item{n_subjects}{Integer. Total subjects. Default 60.}

\item{n_analytes}{Integer. Number of analytes. Default 10.}

\item{signal_analytes}{Integer, logical, or character vector. Default 3.}

\item{group_effects}{Numeric. Log-scale group effect. Default 0.5.}

\item{covariate_group_cor}{Numeric. Correlation between covariates and group,
creating genuine confounding. Default 0.4.}

\item{heteroscedastic}{Logical. If TRUE, non-reference groups have inflated
variance. Default FALSE.}

\item{outlier_rate}{Numeric in [0, 1). Fraction of observations to replace
with outliers (shifted by 3 SD). Default 0.}

\item{nonlinear_cov}{Logical. If TRUE, adds a quadratic term to the
covariate effect. Default FALSE.}

\item{seed}{Integer or NULL. RNG seed.}

\item{...}{Additional arguments passed to \code{\link{simulate_immunoassay}}.}
}
\value{
An \code{\link{new_immuno_sim}} object. The \code{$meta$ancova}
element stores ANCOVA-specific parameters including outlier indices
and nonlinear covariate specification.
}
\description{
Generates group comparison data with covariates correlated with group
assignment, enabling tests of confounding adjustment. Supports nonlinear
covariate effects, heteroscedastic residuals, and outlier injection.
}
\examples{
\dontrun{
sim <- simulate_ancova_data(
  n_subjects = 80, covariate_group_cor = 0.4,
  outlier_rate = 0.05, seed = 42
)
print(sim)
}

}
