% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-plsda_model.r
\name{plot.plsda_model}
\alias{plot.plsda_model}
\title{Plot PLS-DA model results}
\usage{
\method{plot}{plsda_model}(
  x,
  plot_type = c("scores", "loadings", "vip", "biplot"),
  components = c(1, 2),
  color_by = NULL,
  shape_by = NULL,
  add_ellipse = TRUE,
  vip_threshold = 1,
  top_n = NULL,
  arrow_scale = 0.7,
  label_repel = TRUE,
  ...
)
}
\arguments{
\item{x}{A plsda_model object from plsda_fit()}

\item{plot_type}{character string specifying plot type. Options:
"scores" (sample score plot), "loadings" (cytokine loadings),
"vip" (Variable Importance in Projection), "biplot" (scores + loadings overlay)}

\item{components}{numeric vector of length 2 specifying which components to plot.
Default: c(1, 2)}

\item{color_by}{character string naming the metadata column for coloring points.
If NULL, uses the response variable. Default: NULL}

\item{shape_by}{character string naming the metadata column for point shapes.
Default: NULL}

\item{add_ellipse}{logical indicating whether to add 95\% confidence ellipses
for score plots. Default: TRUE}

\item{vip_threshold}{numeric threshold for highlighting important variables
in VIP plots. Default: 1.0}

\item{top_n}{integer specifying number of top cytokines to show in loading
and VIP plots. If NULL, shows all. Default: NULL}

\item{arrow_scale}{numeric scaling factor for loading arrows in biplot.
Default: 0.7}

\item{label_repel}{logical indicating whether to use ggrepel for labels
in biplot. Default: TRUE}

\item{...}{Additional arguments passed to specific plot functions}
}
\value{
A ggplot object
}
\description{
Generate publication-quality visualizations for PLS-DA model results.
Supports multiple plot types including score plots, loading plots,
VIP plots, and biplots.
}
\examples{
\dontrun{
# Fit model
model <- plsda_fit(preprocessed, response_var = "group")

# Score plot
plot(model, plot_type = "scores", color_by = "group")

# Loading plot
plot(model, plot_type = "loadings", top_n = 20)

# VIP plot
plot(model, plot_type = "vip", vip_threshold = 1)

# Biplot
plot(model, plot_type = "biplot", color_by = "group", shape_by = "site")
}

}
